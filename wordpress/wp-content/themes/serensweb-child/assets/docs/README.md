# CV asset

The Download CV button in the contact section links to `seren-cv.pdf` in this folder.

The real CV is in place as `seren-cv.pdf` (exact filename), so the download works.
To update it, replace this file, keeping the same name. Self-hosted on purpose: no
plugin, no CDN.
